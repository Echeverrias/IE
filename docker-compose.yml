version: '3'

services:

  nginx:
    build: ./nginx
    ports:
      - "8000:80"
    volumes:
      - static_volume:/IE/staticfiles
    depends_on:
      - web

  db:
    image: mysql:8.0
    ports:
      - '3308:3306'
    environment:
       MYSQL_DATABASE: 'ie'
       MYSQL_ROOT_PASSWORD: 'root'
    restart: always
    volumes:
      - mysql_data:/var/lib/mysql
  web:
    build: ./src
    command: >
       sh -c "/wait
       && python manage.py makemigrations
       && python manage.py migrate
       && python manage.py initadmin root root@ie.com root
       && python manage.py initdb
       && gunicorn ie_django.wsgi:application --bind 0.0.0.0:8000"
    volumes:
      - ./src:/IE/src
      - static_volume:/IE/staticfiles
    depends_on:
      - db
    environment:
      - WAIT_HOSTS=db:3306
      - WAIT_HOSTS_TIMEOUT=300
      - WAIT_SLEEP_INTERVAL=30
      - WAIT_HOST_CONNECT_TIMEOUT=30

volumes:
  static_volume:
  mysql_data:
